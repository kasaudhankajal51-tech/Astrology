/**
 * Tailwind UI playground — edit THIS file to test your own components.
 *
 * 1. Build your component here (or import it and render it below).
 * 2. Open /tailwind in the browser.
 * 3. If the canary box on the page has orange padding but your UI does not,
 *    the issue is in your component classes — not the Tailwind setup.
 */

export default function TailwindTestPlayground() {
  return (
    <div>
      {/* Paste or build your test UI below */}
    </div>
  );
}
